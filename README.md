Необходимые команды для запуска

`python -m venv venv`
или
`python3 -m venv venv`
 
Для windows `.\venv\Scripts\activate`

Для linux `source venv/bin/activate`

установка зависимостей
`pip install -r requirements.txt`

И запуск ячеек по порядку
